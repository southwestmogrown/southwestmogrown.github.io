// Shared identity constants used across components
export const IDENTITY = {
  name: 'WILKEY.SHANE',
  version: 'V2.0.48',
  role: 'SYSTEMS ARCHITECT',
}

export const OS_VERSION = `${IDENTITY.version} // ${IDENTITY.role}`
